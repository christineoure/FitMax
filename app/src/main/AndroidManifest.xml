<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/back_gradient"
        android:supportsRtl="true"
        android:name=".FitMax"
        android:theme="@style/Theme.FitMax">
        <activity
            android:name=".ui.home_and_splash.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login_sign_up.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login_sign_up.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home_and_splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>